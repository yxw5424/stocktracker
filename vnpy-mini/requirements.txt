# --- 界面后端（mock 模式只需要这三个，能立刻跑起来）---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# --- research 研究层（因子/回测/验证）需要 ---
numpy>=1.24
pandas>=2.0

# --- AI 顾问（接 Claude）需要；无 key 时自动退回 mock，可不装 ---
anthropic>=0.40

# --- 真实 A股数据（可选；装不上可改用 tushare/qlib）---
# akshare

# --- live 模式（真·实时模拟盘）额外需要，按需手动安装 ---
# 见 README：建议在 Windows + Python 3.10 下安装
# vnpy>=4.0
# vnpy_ctp        # CTP/SimNow 网关（含编译好的 C++ 接口）
